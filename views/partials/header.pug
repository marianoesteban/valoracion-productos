.navbar.navbar-light.fixed-top.navbar-expand-lg.bg-light
  .container
    a.navbar-brand(href='/')
      i.fas.fa-poll
      | Valpro
    button.navbar-toggler(type='button', data-toggle='collapse', data-target='.navbar-collapse')
      span.sr-only Toggle navigation
      span.navbar-toggler-icon
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li.nav-item(class=(title === 'Inicio') ? 'active' : undefined)
          a.nav-link(href='/') Inicio
        if user
          if user.role === 'admin'
            li.nav-item(class=(title === 'Categorías') ? 'active' : undefined)
              a.nav-link(href='/categorias') Categorías
            li.nav-item(class=(title === 'Marcas') ? 'active' : undefined)
              a.nav-link(href='/marcas') Marcas
            li.nav-item(class=(title === 'Productos') ? 'active' : undefined)
              a.nav-link(href='/productos') Productos
      ul.nav.navbar-nav.ml-auto
        if !user
          li.nav-item(class=(title === 'Iniciar sesión') ? 'active' : undefined)
            a.nav-link(href='/iniciar-sesion') Iniciar sesión
          li.nav-item(class=(title === 'Registro') ? 'active' : undefined)
            a.nav-link(href='/registro') Registrarse
        else
          li.dropdown.nav-item
            a.dropdown-toggle.nav-link(href='#', data-toggle='dropdown')
              span= user.username
            div.dropdown-menu
              a.dropdown-item(href='/cerrar-sesion') Cerrar sesión
