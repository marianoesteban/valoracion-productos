extends ../layout

block content
  .pb-2.mt-2.mb-4.border-bottom
    h3 Editar producto
  +producto-form('Editar producto', ['far', 'fa-edit'], categorias, marcas, producto.imagen.data && `data:${producto.imagen.mediaType};base64,${producto.imagen.data.toString('base64')}`, producto.categoria.toString(), producto.marca.toString(), producto.modelo)
